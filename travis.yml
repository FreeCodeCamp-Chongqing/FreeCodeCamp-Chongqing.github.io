language: node_js

cache:
  directories:
    - "node_modules"
    
install:
  - yarn

before_script:
  - export TZ='Asia/Shanghai'
  - yarn global add gulp
  
script:
  - hexo clean && hexo -g
  - hexo -d
